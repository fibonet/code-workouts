class BankAccount {
    constructor() {
        // Initialize balance
    }

    deposit(amount) {
        // Add amount to balance
    }

    withdraw(amount) {
        // Subtract if enough balance
    }

    getBalance() {
        // Return balance
    }
}

let account = new BankAccount();
let transactions = [["deposit",100], ["withdraw",50], ["withdraw",100]];
// TODO: process transactions
